kind: pipeline
type: docker
name: default

trigger:
  event:
    - push

steps:
  - name: build-apk
    image: cirrusci/flutter:stable
    settings:
      cn_net: true
      source: "."
      commands:
        - flutter build apk